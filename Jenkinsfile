pipeline {
  agent any
  stages {
    stage('daily') {
      steps {
        sh 'echo build'
      }
    }
    stage('deploy') {
      steps {
        sh 'echo no'
      }
    }
    stage('test') {
      steps {
        sh 'echo test'
      }
    }
  }
}